meta {
  name: Authorization server
  type: http
  seq: 2
}

post {
  url: {{URI}}/api/auth
  body: none
  auth: inherit
}

script:post-response {
  let data = res.getBody();
  
  bru.setEnvVar("token", "Bearer " + data.token);
}
